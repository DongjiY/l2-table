{
  "name": "l2-table-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "sample/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish",
    "dev": "npm run dev --workspace @dongjiy/l2-table",
    "serve": "serve . -l 3000",
    "dev:all": "concurrently \"npm run dev --workspace @dongjiy/l2-table\" \"npm run serve\""
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "concurrently": "^9.2.1",
    "serve": "^14.2.5"
  }
}
